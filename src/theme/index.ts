import { extendTheme } from '@chakra-ui/react';

// TODO: customizar aqui o thema
const custonTheme = extendTheme({
	fonts: {
		body: 'Roboto, sans-serif',
    heading: 'Roboto, sans-serif',
  },
});

export default custonTheme;
